package headfirst.factory.factory;

import org.junit.Test;

/**
 * 工厂模式：以自行车商店为例
 * 自行车商店通过 厂商获得自行车，再通过包装自行车，再卖出去
 * 1.将商店设为 抽象类，将生产自行车的工厂分给子类实现，以实现获得不同的自行车
 */
public class MainTest {
    @Test
    public void test(){
        //BikeFactory bikeFactory = new FeiGeBikeFactory();
        //Bike bike = bikeFactory.createBike();
    }
}
